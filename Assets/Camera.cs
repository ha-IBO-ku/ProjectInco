using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Camera : MonoBehaviour
{
    //GameObject PlayerSP;
    
    // Start is called before the first frame update
    void Start()
    {
        //PlayerSP = GameObject.Find("Sphere");
        //this.transform.parent = PlayerSP.transform;
    }

    // Update is called once per frame
    //void Update()
    //{
        
    //}

    void FixedUpdate()
    {
        //this.transform.parent = PlayerShipPrefab.transform;
    }
    
}
